import Ember from 'ember';

/**
 * This component will create the appropriate panel-heading div needed for a bootstrap panel:
 * ```
 * <div class="panel-heading">
 *     //content here...
 * </div>
 * ```
 */
export default Ember.Component.extend({
    classNames: ['panel-heading']
});
